<!DOCTYPE html>
<html>
  <head>
    <title>Community Calendar</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Spectral:wght@400;600;700&family=DM+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <header class="site-header">
      <div class="header-content">
        <div class="logo">
          <div class="logo-icon">📅</div>
          <div class="logo-text">
            <h1>Community Calendar</h1>
            <p>120 Members</p>
          </div>
        </div>
        <div class="header-actions">
          <% if user_signed_in? && current_user.calendars.any? %>
            <%= link_to edit_calendar_path(@calendar || current_user.calendars.first), class: "btn-custom btn-custom-secondary" do %>
              <span>⚙️</span> Settings
            <% end %>
          <% else %>
            <button class="btn-custom btn-custom-secondary" disabled style="opacity: 0.5; cursor: not-allowed;">
              <span>⚙️</span> Settings
            </button>
          <% end %>
          <% if user_signed_in? %>
            <%= button_to calendar_index_path, method: :post, class: "btn-custom btn-custom-primary" do %>
              <span>+</span> Add Calendar
            <% end %>
            <%= link_to destroy_user_session_path, data: { turbo_method: :delete }, class: "btn-custom btn-custom-secondary" do %>
              Sign Out
            <% end %>
          <% else %>
            <%= link_to new_user_session_path, class: "btn-custom btn-custom-primary" do %>
              <span>+</span> Add Calendar
            <% end %>
          <% end %>
        </div>
      </div>
    </header>

    <main>
      <%= render "shared/flash" %>
      <%= yield %>
    </main>

    <% if user_signed_in? %>
      <%= render "chat/panel" %>
    <% end %>
  </body>
</html>
